$DataComponent.register("vorant_ingot_mobkill",
    $DataComponent.builder()
        .type("DOUBLE")
        .persistent(true)
        .networked(true)
)

$DataComponent.register("vorant_ingot_playerkill",
    $DataComponent.builder()
        .type("DOUBLE")
        .persistent(true)
        .networked(true)
)

$DataComponent.register("add_enchantment_count",
    $DataComponent.builder()
        .type("DOUBLE")
        .persistent(true)
        .networked(true)
)

$DataComponent.register("demonite_ingot_blocks",
    $DataComponent.builder()
        .type("DOUBLE")
        .persistent(true)
        .networked(true)
)
